// Background Color
export const THEME_MAIN_BACKGROUND_COLOR = (theme) =>
  theme.palette.background.default;
export const THEME_SECONDARY_BACKGROUND_COLOR = (theme) =>
  theme.palette.background.paper;

export const THEME_LINK_COLOR = (theme) => theme.palette.link;

// colors
export const THEME_CONTRAST_TEXT_COLOR = (theme) =>
  theme.palette.primary.contrastText;
export const THEME_MAIN_COLOR = (theme) => theme.palette.primary.main;
export const THEME_DELETE_COLOR = (theme) => theme.palette.error.main;
export const THEME_SUCCESS_COLOR = (theme) => theme.palette.success.main;
export const THEME_SECONDARY_MAIN_COLOR = (theme) =>
  theme.palette.secondary.main;
